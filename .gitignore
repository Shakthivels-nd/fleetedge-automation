# Environment / secrets
.env

# Python
__pycache__/
*.pyc
.pytest_cache/

# Local virtualenvs
fe_env/
.venv/
venv/

# Test / report output (regenerated by every run)
pytest.log
report/
report.html
src/reports/
src/FE_latest_report/
src/FE_latest_report.zip
src/*_report.zip
src/*_report/
src/tests/pytest.log
src/tests/.pytest_cache/

# JIRA credentials (env vars only — never a committed config file)
jira_config.ini
